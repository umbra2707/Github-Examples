name: workflow-command

on: ['push']

jobs: 
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: "group logging"
        runs: |
          echo "::group::My group message"
          echo "Message 1"
          echo "Message 2"
          echo "::endgroup::"
