name: context

on: ['push']

jobs: 
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: "step"
        run: echo "hello $MY_ACTION"
        env:
          MY_ACTION: ${{ github.action }}